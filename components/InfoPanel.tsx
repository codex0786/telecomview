"use client";
// Legacy component — functionality moved to InfoCards.tsx
// Kept as empty stub to avoid import errors from any remaining references
export default function InfoPanel() { return null; }
